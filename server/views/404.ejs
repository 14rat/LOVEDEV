<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Site não encontrado - 404</title>
    <link href="https://fonts.googleapis.com/css2?family=Dancing+Script:wght@400;600&family=Open+Sans:wght@300;400&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Open Sans', sans-serif;
            background: linear-gradient(135deg, #fff5f8 0%, #ffc3d8 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0;
        }
        .container {
            text-align: center;
            padding: 40px;
            background: white;
            border-radius: 20px;
            box-shadow: 0 20px 40px rgba(0,0,0,0.1);
            max-width: 500px;
        }
        .error-code {
            font-family: 'Dancing Script', cursive;
            font-size: 6rem;
            color: #ff6b9d;
            margin-bottom: 20px;
        }
        .error-message {
            font-size: 1.2rem;
            color: #555;
            margin-bottom: 30px;
        }
        .back-link {
            display: inline-block;
            padding: 12px 24px;
            background: #ff6b9d;
            color: white;
            text-decoration: none;
            border-radius: 25px;
            font-weight: 600;
            transition: background 0.3s;
        }
        .back-link:hover {
            background: #e55a87;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="error-code">404</div>
        <div class="error-message">
            💔 <%= message || 'Site não encontrado' %>
        </div>
        <a href="<%= typeof baseDomain !== 'undefined' ? `https://app.${baseDomain}` : '#' %>" class="back-link">
            Criar Meu Site Romântico
        </a>
    </div>
</body>
</html>
